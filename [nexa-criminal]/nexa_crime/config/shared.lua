NexaCrimeConfig = {
    autoMigrate = true,
    defaultHeatDecay = 1,
    maximumHeat = 100,
    minimumReputation = -1000,
    maximumReputation = 1000,
    defaultCooldownSeconds = 900,
    callbacks = {
        enabled = true,
        rateLimitMs = 1200
    }
}
