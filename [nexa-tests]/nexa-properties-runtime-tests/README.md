# nexa-properties-runtime-tests

Runtime smoke tests for the property foundation.

Run from server console:

```text
nexa_test_properties_runtime all
```

Player execution requires ACE `nexa.tests.properties_runtime`. Live FXServer execution is required before marking runtime behavior as accepted.
