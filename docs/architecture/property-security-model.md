# Property Security Model

Trust boundary: server owns property ID resolution, owner, lease, residents, keys, buckets, storage, garages, furniture persistence and security state.

Network callbacks must bind to actual source and resolved character. No client-supplied bypass flag, owner ID, lease ID, bucket, storage ID or garage ID is trusted without server access checks.
