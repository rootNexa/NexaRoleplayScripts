# Admin Notes

Admin notes are stored in `nexa_admin_notes`.

Notes have category, visibility, soft delete, target account and optional target character. Content is length-limited and strips basic HTML delimiters before storage.
