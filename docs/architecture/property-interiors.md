# Property Interiors

Interior types are `mlo`, `shell`, `routing_instance` and `exterior_only`.

Definitions include entry, exit, spawn position, routing strategy, furniture bounds, storage points, wardrobe points, garage links, status and metadata.
