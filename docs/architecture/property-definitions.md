# Property Definitions

Definitions are creator-owned templates. They include key, label, type, status, purchase price, rent amount, entrance, exterior, interior definition, garage definition, storage config, security config, settings and metadata.

Definitions move through `draft`, `active`, `disabled`, `archived` and `deleted`. Existing instances are not silently rewritten by definition changes.
