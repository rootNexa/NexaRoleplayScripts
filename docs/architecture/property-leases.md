# Property Leases

Lease status values are `draft`, `active`, `overdue`, `suspended`, `terminated`, `expired` and `evicted`.

Only rentable properties can create a lease. One active primary lease per property is allowed. Lease history is retained after termination or eviction.
