# Inventory Operations

`nexa_inventory` wird nach `nexa_playerstate` gestartet. `nexa_items` sollte vorher laufen, wenn Itemdefinitionen nicht aus dem internen Uebergangskatalog kommen sollen. Runtime-Testresource nicht produktiv autostarten.
