# Playerstate Security

- Client cannot set player lifecycle state.
- Client cannot create spawn tokens.
- Client cannot choose spawn position.
- Client cannot set bucket.
- Client cannot choose account or character ID.
- Position snapshots are hints and are validated.
- Spawn token is source-bound, character-bound, one-time and TTL-bound.
