# Property Security

Security tracks alarm state, lock state and security events. Alarm states are `none`, `inactive`, `armed`, `triggered`, `disabled` and `maintenance`.

Dispatch integration is event-based and deferred.
