# Property Routing Buckets

Buckets are reserved server-side and bound to property interior instances. Entry uses a one-time source/character-bound token with TTL.

Disconnect and resource stop cleanup must prevent orphaned occupants and stale tokens.
