# Item Registry

Die Registry laedt veroeffentlichte Definitionen in einen versionierten Cache. Updates invalidieren den Cache. Fehlerhafte Drafts blockieren den Start nicht; fehlerhafte published Definitionen werden geloggt und nicht still korrigiert.
