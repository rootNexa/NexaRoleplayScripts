# Routing Buckets

Bucket ownership is server-side.

APIs:

- `Buckets.Get`
- `Buckets.Set`
- `Buckets.Reset`
- `Buckets.Validate`
- `Buckets.RegisterRange`

Default bucket is `0`. Future systems must register ranges instead of inventing random buckets.
