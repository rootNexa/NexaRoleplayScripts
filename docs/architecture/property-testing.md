# Property Testing

Static validators cover forbidden dependencies, persistence, security boundaries, ownership, leases, keys, interiors, storage, security and runtime harness shape.

Live FXServer tests are exposed through `nexa_test_properties_runtime [suite]` and must not be reported as passed unless executed in a running server.
