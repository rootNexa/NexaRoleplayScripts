# Shop Data Model

Tables: `nexa_shop_definitions`, `nexa_shop_items`, `nexa_shop_transactions`, `nexa_shop_stock_movements`, `nexa_shop_deliveries`, `nexa_shop_audit`.

All writes use Core DB abstraction. Old `shops` and `shop_items` tables are legacy compatibility only.
