# Shop Stock

Stock modes: `infinite`, `finite_virtual`, `inventory_backed`, `production_backed`.

Finite stock reserves, commits and releases through server-owned stock movement records. Infinite stock is only valid for shop types that allow it.
