# Item Security

Clients duerfen keine Definitionen, Handler, Gewichte, Stackregeln, serverOnly-Metadaten oder Events bestimmen. `load`, `loadstring`, `dofile` und freie Events aus Datenbankwerten sind verboten.
