# Admin Noclip

Noclip is duty-gated and server-authorized.

The server owns active state and speed level. The client only reads inputs while the server says noclip is active. Noclip does not grant godmode.
