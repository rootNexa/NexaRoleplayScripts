# Property Troubleshooting

Common checks:

- verify `nexa-core` database health
- verify `nexa_economy`, `nexa_inventory` and `nexa_garages` are started
- check property audit for failed mutation
- inspect lease status before rent payment
- inspect entry token TTL and source/character binding for interior entry issues
