# Property Doors

Doors are server-defined and identified by a property-local door key. Persistent lock state lives in `nexa_property_security`.

Clients can apply visual door state, but cannot authoritatively persist lock state.
