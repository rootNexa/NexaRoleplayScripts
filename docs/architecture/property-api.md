# Property API

Public exports cover properties, leases, residents, keys, interiors, storage, garages and security. Mutating exports accept actor/context tables with reason, source resource and correlation ID.

Callbacks are added through Nexa callback conventions in later UI-facing work and must wrap these server exports.
