# Inventory Security

Clients koennen keine Items erzeugen, keine Menge autorisieren, kein Gewicht setzen, keine fremden Inventory-IDs frei nutzen und keine Metadaten blind persistieren. Netzwerkcallbacks binden immer an die echte FiveM-source.
