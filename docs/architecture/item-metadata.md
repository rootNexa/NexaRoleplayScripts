# Item Metadata

Metadaten werden serverseitig validiert. Unterstuetzte primitive Typen: string, number, integer, boolean, enum, object, array, date und identifier. `serverOnly` und `sensitive` Felder werden nicht an Clients gesendet.
