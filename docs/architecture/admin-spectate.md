# Admin Spectate

Spectate state is server-side. Starting spectate stores original position and target. Stopping restores client state and attempts to return the actor.

The client cannot start spectate itself.
