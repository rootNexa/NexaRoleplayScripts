# Property Residents

Resident types are `owner`, `tenant`, `roommate`, `guest`, `organization_member` and `service_access`.

Residents have status, permissions, inviter, join/leave timestamps and metadata. Removal does not delete storage contents; it revokes dependent access.
