# Property Storage

Storage is represented by server-created inventory ownership such as `property:<id>:<storageKey>`. Access is checked by property rights before `nexa_inventory` is used.

Lease end or sale locks access by policy; it does not delete items.
