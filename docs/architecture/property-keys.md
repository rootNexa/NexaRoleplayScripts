# Property Keys

Key types are `owner`, `tenant`, `resident`, `guest`, `temporary`, `organization`, `service` and `admin`.

Keys grant explicit permissions such as enter, exit, lock, unlock, storage, wardrobe, garage, furniture and security actions. Expired or revoked keys do not authorize access.
