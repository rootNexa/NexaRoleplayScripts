# Property Ownership

Owner types are `character`, `organization`, `government` and `system`. Ownership status is `unowned`, `owned`, `for_sale`, `reserved`, `seized` or `archived`.

Ownership changes are recorded in history and audit. Gameplay ownership must come from a controlled sale, transfer or admin action; clients never set owner IDs directly.
