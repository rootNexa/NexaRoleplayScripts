# Shop Catalog

Catalog entries are scoped by shop and item name. Items must exist in `nexa_items` when that resource is available. Buy price, sell price, buy/sell flags, stock model, limits, license and access rules are persisted server-side.
