# nexa_propertykeys

Server-side property key, access and door-lock foundation. It persists key grants, key revocation, door definitions and lock state. Clients never create keys or persist door states directly.
